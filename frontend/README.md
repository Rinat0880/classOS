# Admin Panel для школ/университетов

Фронтенд админ-панель для управления пользователями и группами.

## 🚀 Технологический стек

- **React 18** + **TypeScript**
- **Vite** - сборщик
- **React Router v6** - навигация
- **TanStack Query** - управление серверным состоянием
- **Zustand** - state management
- **Axios** - HTTP клиент
- **Tailwind CSS** - стилизация
- **Lucide React** - иконки
- **React Hook Form** + **Zod** - формы и валидация

## 📦 Установка

```bash
# Установка зависимостей
npm install

# Копирование .env файла
cp .env.example .env

# Настройка API URL в .env
# VITE_API_BASE_URL=http://localhost:8080
```

## 🏃‍♂️ Запуск

```bash
# Development сервер
npm run dev

# Production build
npm run build

# Preview production build
npm run preview
```

## 📁 Структура проекта

```
src/
├── components/        # Переиспользуемые компоненты
│   ├── ui/           # UI компоненты
│   ├── layout/       # Layout компоненты (Sidebar, Header)
│   └── common/       # Общие компоненты
├── pages/            # Страницы приложения
│   ├── Login/        # Страница входа
│   ├── Dashboard/    # Главная страница
│   ├── Groups/       # Управление группами
│   ├── Users/        # Управление пользователями
│   └── Settings/     # Настройки
├── services/         # API сервисы
│   ├── api/          # Axios конфигурация
│   └── auth/         # Auth сервис
├── store/            # Zustand stores
├── hooks/            # Custom hooks
├── types/            # TypeScript типы
├── utils/            # Утилиты
└── constants/        # Константы
```

## 🔐 API Endpoints

### Auth
- `POST /auth/sign-up` - регистрация
- `POST /auth/sign-in` - вход

### Groups
- `GET /api/groups` - все группы
- `POST /api/groups` - создать группу
- `GET /api/groups/:id` - группа по ID
- `PATCH /api/groups/:id` - обновить группу
- `DELETE /api/groups/:id` - удалить группу

### Users
- `GET /api/users/:id` - пользователь по ID
- `PATCH /api/users/:id` - обновить пользователя
- `DELETE /api/users/:id` - удалить пользователя
- `POST /api/users/:id/password` - изменить пароль

### Admin
- `POST /api/admin/sync` - синхронизация с AD
- `GET /api/admin/ad/status` - статус подключения к AD

## 📝 Текущий статус

**Phase 1: Инициализация проекта - ✅ ЗАВЕРШЕНА**

- ✅ Создан проект с Vite + React + TypeScript
- ✅ Настроен Tailwind CSS
- ✅ Настроен роутинг (React Router)
- ✅ Настроен Axios с interceptors
- ✅ Настроен Zustand store
- ✅ Создана базовая структура папок
- ✅ Созданы TypeScript типы
- ✅ Создан MainLayout с Sidebar
- ✅ Созданы заглушки для всех страниц

**Следующая фаза: Phase 2 - Аутентификация**
